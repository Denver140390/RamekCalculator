<UserControl x:Class="RamekCalculator.Views.CalculatorDisplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Log}" FontSize="14"/>
            <TextBlock Text="{Binding Input}" FontSize="18"/>
        </StackPanel>
    </Grid>
</UserControl>
