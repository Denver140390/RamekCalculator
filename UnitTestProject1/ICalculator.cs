namespace CalculatorTest
{
    public interface ICalculator
    {
        decimal Add(decimal v1, decimal v2);

        decimal Substract(decimal v1, decimal v2);

        decimal Multiply(decimal v1, decimal v2);

        decimal Divide(decimal v1, decimal v2);
    }
}